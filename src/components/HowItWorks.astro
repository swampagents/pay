---
const steps = [
  {
    number: '01',
    title: 'Establish Firebase',
    description: 'Deploy the SP-700 terminal or our white-label mobile app. Connect to your merchant wallet. Your base of operations is live.',
    status: 'BASE CAMP',
  },
  {
    number: '02',
    title: 'Initiate Contact',
    description: 'Customer taps their card, phone, or crypto wallet. The terminal initiates a local handshake — no data leaves the perimeter.',
    status: 'CONTACT',
  },
  {
    number: '03',
    title: 'Secure the Perimeter',
    description: 'Transaction authorized and encrypted on-device. The secure enclave locks it down. Nothing gets out.',
    status: 'FORTIFIED',
  },
  {
    number: '04',
    title: 'Mission Complete',
    description: 'Funds settle directly to your wallet. Transaction data is purged from the terminal. No trace. Charlie Mike.',
    status: 'ACCOMPLISHED',
  },
];
---
<section class="relative overflow-hidden border-t border-olive/20 bg-bunker py-28" aria-label="How it works">
  <img
    src={`${import.meta.env.BASE_URL}/artillery.jpg`}
    alt=""
    class="war-photo-bg-intense"
    loading="lazy"
    aria-hidden="true"
  />
  <div class="war-photo-overlay" aria-hidden="true"></div>
  <div class="film-grain absolute inset-0 z-[2]" aria-hidden="true"></div>

  <div class="relative z-10 mx-auto max-w-7xl px-6">
    <div class="text-center">
      <p class="section-label-military">Standard Operating Procedure</p>
      <h2 class="font-stencil text-3xl tracking-wide text-white sm:text-4xl">
        Four steps. <span class="text-phosphor crt-glow">Zero exposure.</span>
      </h2>
    </div>

    <div class="mt-16 grid gap-6 md:grid-cols-2 lg:grid-cols-4">
      {steps.map(({ number, title, description, status }) => (
        <div class="group relative">
          <div class="rounded border border-olive/20 bg-midnight/70 p-6 backdrop-blur-sm transition-colors hover:border-phosphor/30">
            <div class="flex items-center justify-between">
              <span class="font-stencil text-3xl text-olive/40 transition-colors group-hover:text-phosphor/50">{number}</span>
              <span class="font-mono text-[10px] text-khaki">[{status}]</span>
            </div>
            <h3 class="mt-4 font-stencil text-base text-white">{title}</h3>
            <p class="mt-3 text-sm leading-relaxed text-smoke">{description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
